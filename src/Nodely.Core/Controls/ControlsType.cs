namespace Nodely.Controls;

/// <summary>When a model's adornment controls are shown.</summary>
public enum ControlsType
{
    /// <summary>Shown while the pointer hovers the model.</summary>
    OnHover,

    /// <summary>Shown while the model is selected.</summary>
    OnSelection
}
