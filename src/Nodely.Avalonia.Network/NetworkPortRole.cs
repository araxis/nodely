namespace Nodely.Avalonia.Network;

/// <summary>Semantic roles for network ports.</summary>
public enum NetworkPortRole
{
    Lan,
    Wan,
    Uplink,
    Downlink,
    Management,
    Service,
    Client,
}
